<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atakmap.android.cotviewer.plugin"
    android:versionCode="1762495126"
    android:versionName="1.0 (cc085314) - [5.4.0]" >

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="31" />

    <application
        android:allowBackup="false"
        android:debuggable="true"
        android:description="@string/app_desc"
        android:extractNativeLibs="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="plugin-api"
            android:value="com.atakmap.app@5.4.0.CIV" />
        <meta-data
            android:name="app_desc"
            android:value="@string/app_desc" />

        <!-- Discovery entry for ATAK -->
        <activity
            android:name="com.atakmap.app.component"
            android:exported="true" >
            <intent-filter android:label="@string/app_name" >
                <action android:name="com.atakmap.app.component" />
            </intent-filter>
        </activity>

        <!-- No service required for CoT viewer -->
    </application>

</manifest>