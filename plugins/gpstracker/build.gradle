buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath files('../../../../atak-gradle-takdev.jar')
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
